name: hadoop-nodemanager
summary: YARN slave node (NodeManager) for Apache Bigtop platform
maintainer: Juju Big Data <bigdata@lists.ubuntu.com>
description: >
  Hadoop is a software platform that lets one easily write and
  run applications that process vast amounts of data.

  This charm manages the YARN slave node (NodeManager).
tags: ["applications", "bigdata", "bigtop", "hadoop", "apache"]
provides:
  resourcemanager:
    interface: mapred-slave
